fastapi==0.115.6
uvicorn[standard]==0.34.0

# Veritabanı: SQLAlchemy (ORM) + psycopg (Postgres sürücüsü)
sqlalchemy==2.0.36
psycopg[binary]==3.2.3

# Ayarlar env'den okunur, eksikse açılışta hata (fail-fast)
pydantic-settings==2.7.0

# RabbitMQ istemcisi (user.registered tüketicisi)
pika==1.3.2

# NOT: jwt/argon2 bilerek YOK — token doğrulama gateway'de (ForwardAuth),
# bu servis kimliği X-User-Id başlığından alır.
